# Copilot Instructions

## Project Guidelines
- User prefers the Index page to keep `public override string RenderMode => RenderModes.Static;` (static render mode).
- Follow Oqtane playbook governance rules and never modify files in the oqtane.framework folder.
- Use specific culture resource file naming (e.g., Index.it-IT.resx) for this module's localization guidance.